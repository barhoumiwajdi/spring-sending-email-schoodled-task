server.port=8080
debug=false

# ================== Configure Base URL for REST API =============================
# With Spring Boot 1.2+ (<2.0) all it takes is a single property in application.properties:
#spring.data.rest.basePath=/api/v1
# For Spring Boot 2.x, use
server.servlet.context-path=/api/v1

# Mail properties
spring.mail.protocol=smtp
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=wajdi.barhoumi26@gmail.com
spring.mail.password=yjgfihfzjrrmzaxj


# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true

#Below Properties are needed for maintaining the session.
#Two Step verification should not be enabled for gmail.

